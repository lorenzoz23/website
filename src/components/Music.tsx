import React from 'react';
import { Box, Button } from 'grommet';
import { Music as MusicIcon } from 'grommet-icons';

const Music = () => {
  return (
    <Box
      flex
      direction="column"
      align="center"
      justify="center"
      background="home"
    >
      under construction
      <Button icon={<MusicIcon />} margin="small" />
    </Box>
  );
};

export default Music;
